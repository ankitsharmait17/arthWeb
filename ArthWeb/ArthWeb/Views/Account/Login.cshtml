@using ArthWeb.Models
@model LoginModel
@{
    ViewBag.Title = "Log in";
    Layout = "~/Views/Shared/MatLayout.cshtml";
}
<div class="container-fluid">
    <div class="row">
        <div class="col l4 s1 m2"></div>
        <div class="col l4 s10 m8">
            <div class="card">
                <div class="card-image">
                    <img src="https://www.tate.org.uk/art/images/work/N/N04/N04948_10.jpg">
                    <span class="card-title">Sign in/Sign up</span>
                </div>
                <div class="card-content">
                    <div class="row">
                        <div class="col s12">
                            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { onsubmit = "return validate();" }))
                            {
                                @Html.AntiForgeryToken()
                                <div class="input-field">
                                    @Html.TextBoxFor(m => m.UserName, new { @required = "required", @id = "uname", @maxlength = "254", @type = "email", @class = "validate" })
                                    @Html.LabelFor(m => m.UserName)
                                </div>
                                <div class="input-field">
                                    @Html.PasswordFor(m => m.Password, new { @required = "required", @id = "pass", @class = "validate" })
                                    @Html.LabelFor(m => m.Password)
                                </div>
                                <label>
                                    <input data-val="true" data-val-required="The Remember me? field is required." id="RememberMe" name="RememberMe" type="checkbox" value="true">
                                    <span>Remember Me?</span>
                                </label>
                                <br />
                                <br />
                                <input type="submit" value="Sign in" class="btn" />

                            }
                        </div>
                    </div>

                 </div>
                <div class="card-action">
                    @Html.ActionLink("Register as a new user", "Register", "Account")
                    @Html.ActionLink("Forgot Password", "ForgotPassword", "Account")
                </div>
            </div>
        </div> 
        <div class="col l4 s1 m2"></div>
        </div>
    </div>
<script>
    function validate() {
        var uname = $('#uname').val();
        var pass = $('#pass').val();
        if (uname = "") {
            showWarningAlert("Username cannot be empty"); return false;
        }
        if (pass.length < 8) {
            showWarningAlert("Minimum length of password is 8."); return false;
        }
        return true;
    }
</script>